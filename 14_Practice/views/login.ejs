<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title>Login</title>
  </head>
  <body
    class="bg-zinc-900 text-white flex items-center justify-center min-h-screen"
  >
    <div
      class="bg-zinc-800/60 backdrop-blur-lg p-10 rounded-2xl shadow-2xl w-full max-w-md border border-zinc-700"
    >
      <h2 class="text-3xl font-semibold mb-8 text-center text-blue-400">
        Login
      </h2>
      <form action="/login" method="post" class="space-y-5">
        <input
          class="w-full bg-zinc-900 border border-zinc-700 focus:border-blue-500 focus:ring-2 focus:ring-blue-400 outline-none p-3 rounded-lg placeholder-gray-400 transition"
          type="email"
          placeholder="Email"
          name="email"
          required
        />
        <input
          class="w-full bg-zinc-900 border border-zinc-700 focus:border-blue-500 focus:ring-2 focus:ring-blue-400 outline-none p-3 rounded-lg placeholder-gray-400 transition"
          type="password"
          placeholder="Password"
          name="password"
          required
        />
        <button
          type="submit"
          class="w-full bg-blue-600 hover:bg-blue-700 transition-all duration-200 p-3 rounded-lg font-semibold tracking-wide shadow-md hover:shadow-blue-500/30"
        >
          Login
        </button>
      </form>
    </div>
  </body>
</html>
